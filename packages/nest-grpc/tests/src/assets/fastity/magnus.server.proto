package fastity;
syntax = "proto3";
message Empty{
}

message Result{
    int32 result = 0;
}
message AddInput{
    int32 a = 0;
    int32 b = 2;
}
service Query{
    rpc add(AddInput) returns (Result);
}